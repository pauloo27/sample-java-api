# HTTP port for the application
httpPort: 8080

replicaCount: 1

image:
  repository: ghcr.io/pauloo27/sample-java-api
  pullPolicy: IfNotPresent
  # Tag will be set on GHA!

service:
  type: ClusterIP

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 256Mi

livenessProbe:
  httpGet:
    path: /health
readinessProbe:
  httpGet:
    path: /health
